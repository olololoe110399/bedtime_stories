import 'package:flutter/foundation.dart';

class LogConstants {
  const LogConstants._();

  static const enableLog = kDebugMode;
  static const isPrettyJson = kDebugMode;

  /// navigator observer
  static const enableNavigatorObserverLog = kDebugMode;
}
